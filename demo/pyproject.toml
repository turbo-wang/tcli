[project]
name = "openclaw-media-sidecar-demo"
version = "0.1.0"
description = "Reference HTTP sidecar for Open WebUI + tcli login QR"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
